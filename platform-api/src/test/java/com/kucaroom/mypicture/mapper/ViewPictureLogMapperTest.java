package com.kucaroom.mypicture.mapper;import com.kucaroom.mypicture.domain.ViewPictureLog;import lombok.extern.slf4j.Slf4j;import org.junit.Test;import org.junit.runner.RunWith;import org.springframework.beans.factory.annotation.Autowired;import org.springframework.boot.test.context.SpringBootTest;import org.springframework.test.context.junit4.SpringRunner;import static org.junit.Assert.*;@Slf4j@SpringBootTest@RunWith(SpringRunner.class)public class ViewPictureLogMapperTest {    @Autowired    private ViewPictureLogMapper mapper;    @Test    public void findByUserViewLog() {        ViewPictureLog viewPictureLog = mapper.findByUserViewLog(7612,121,2);        log.info("结果：{}",viewPictureLog.toString());    }}