package com.kucaroom.mypicture.service.impl;import com.kucaroom.mypicture.domain.CollectPictureLog;import com.kucaroom.mypicture.service.CollectPictureLogService;import org.junit.Assert;import org.junit.Test;import org.junit.runner.RunWith;import org.springframework.beans.factory.annotation.Autowired;import org.springframework.boot.test.context.SpringBootTest;import org.springframework.test.context.junit4.SpringRunner;import static org.junit.Assert.*;@RunWith(SpringRunner.class)@SpringBootTestpublic class CollectPictureLogServiceImplTest {    @Autowired    private CollectPictureLogService service;    @Test    public void create() {        Integer pictureItemId = 100;        Integer userId = 7607;        CollectPictureLog log = service.create(userId,pictureItemId);        Assert.assertNotNull(log);        Assert.assertEquals(userId,log.getUserId());    }}