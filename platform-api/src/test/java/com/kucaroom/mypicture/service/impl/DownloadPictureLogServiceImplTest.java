package com.kucaroom.mypicture.service.impl;import com.kucaroom.mypicture.domain.DownLoadPictureLog;import com.kucaroom.mypicture.service.DownloadPictureLogService;import org.junit.Assert;import org.junit.Test;import org.junit.runner.RunWith;import org.springframework.beans.factory.annotation.Autowired;import org.springframework.boot.test.context.SpringBootTest;import org.springframework.test.context.junit4.SpringRunner;@SpringBootTest@RunWith(SpringRunner.class)public class DownloadPictureLogServiceImplTest {    @Autowired    private DownloadPictureLogService service;    @Test    public void create() {        DownLoadPictureLog log = service.create(7607,100);        Assert.assertNotNull(log);        Assert.assertEquals(log.getUserId(),(Integer) 7607);    }}