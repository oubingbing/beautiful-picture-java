package com.kucaroom.mypicture.service;import com.kucaroom.mypicture.DTO.PictureDTO;import com.kucaroom.mypicture.domain.Picture;import org.springframework.data.domain.Page;import org.springframework.data.domain.Pageable;import java.util.List;public interface PictureService {    Page<PictureDTO> findByStatus(Pageable pageable,Integer appId,Integer status);    PictureDTO findById(Integer id);}