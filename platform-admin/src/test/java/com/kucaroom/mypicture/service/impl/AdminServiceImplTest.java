package com.kucaroom.mypicture.service.impl;import com.kucaroom.mypicture.DTO.AdminDTO;import com.kucaroom.mypicture.domain.Admin;import com.kucaroom.mypicture.service.AdminService;import com.kucaroom.mypicture.service.AuthService;import com.kucaroom.mypicture.util.MD5Util;import lombok.extern.slf4j.Slf4j;import org.junit.Assert;import org.junit.Test;import org.junit.runner.RunWith;import org.springframework.beans.factory.annotation.Autowired;import org.springframework.boot.test.context.SpringBootTest;import org.springframework.test.context.junit4.SpringRunner;import org.springframework.transaction.annotation.Transactional;@Slf4j@SpringBootTest@RunWith(SpringRunner.class)public class AdminServiceImplTest {    @Autowired    private AdminService adminService;    @Autowired    private AuthService authService;    @Transactional    @Test    public void createTest(){        String password = "bingbing";        AdminDTO adminDTO = new AdminDTO();        adminDTO.setUsername("区叶子");        adminDTO.setEmail("ouzhibingg@outlook.com");        adminDTO.setMobile("13425144866");        adminDTO.setPassword(password);        AdminDTO saveResult = adminService.create(adminDTO);        Assert.assertEquals(adminDTO.getEmail(),saveResult.getEmail());        Assert.assertNotNull(saveResult.getId());        log.info("账号:{},密码：{}",adminDTO.getEmail(),adminDTO.getPassword());        Boolean login = authService.login(adminDTO.getEmail(),password);        Assert.assertEquals(true,login);        String loginEmail = authService.loginUser();        Assert.assertEquals(loginEmail,adminDTO.getEmail());    }    @Test    public void findByEmail() {        AdminDTO adminDTO = adminService.findByEmail("875307054@qq.com");        Assert.assertNotNull(adminDTO);    }    @Test    public void getPassword() {    }}