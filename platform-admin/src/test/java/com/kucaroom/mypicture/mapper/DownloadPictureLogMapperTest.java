package com.kucaroom.mypicture.mapper;import com.kucaroom.mypicture.enums.DownloadPictureType;import lombok.extern.slf4j.Slf4j;import org.junit.Assert;import org.junit.Test;import org.junit.runner.RunWith;import org.springframework.beans.factory.annotation.Autowired;import org.springframework.boot.test.context.SpringBootTest;import org.springframework.test.context.junit4.SpringRunner;import org.springframework.transaction.annotation.Transactional;@Slf4j@SpringBootTest@RunWith(SpringRunner.class)public class DownloadPictureLogMapperTest {    @Autowired    private DownloadPictureLogMapper mapper;    @Transactional    @Test    public void deleteByPictureItemIdAndType() {        Integer result = mapper.deleteByPictureItemIdAndType(92,DownloadPictureType.PICTURE_ITEM.getCode());        log.info("结果：{}",result);        Assert.assertNotEquals((Integer)0,result);    }}